{
  "name": "@isonia/sdk",
  "version": "v0.5.0-alpha.2",
  "description": "Pure TypeScript SDK for IsoniaOS Control Plane REST APIs",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./control-plane-paths": {
      "types": "./dist/control-plane-paths.d.ts",
      "default": "./dist/control-plane-paths.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "test": "pnpm build && node --test test/control-plane-paths.test.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@isonia/types": "github:isoniaos/types#v0.5.0-alpha.2"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
